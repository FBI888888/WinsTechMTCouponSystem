# =====================================================
# WinsTechMT Coupon System 后端配置
# =====================================================

# 应用配置
APP_NAME=MT Coupon System
DEBUG=true

# 数据库配置
DB_HOST=115.190.182.82
DB_PORT=3306
DB_NAME=mt_coupon
DB_USER=root
DB_PASSWORD=ltx123589.
DB_CONNECT_TIMEOUT_SECONDS=10
DB_READ_TIMEOUT_SECONDS=60
DB_WRITE_TIMEOUT_SECONDS=60
DB_POOL_TIMEOUT_SECONDS=30

# JWT 配置
SECRET_KEY=mt_coupon_secret_key_2026_please_change_in_production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# CORS 配置（多个域名用逗号分隔）
CORS_ORIGINS=["http://localhost:5173", "http://localhost:3000"]

# Node.js 路径（用于签名脚本）
NODE_PATH=node

# 扫描配置
SCAN_INTERVAL_MINUTES=30
SCAN_REQUEST_INTERVAL=0.7
SCAN_MAX_RETRIES=3

# 代理配置
PROXY_PORT=8888

# 速率限制配置
RATE_LIMIT_ENABLED=true
RATE_LIMIT_PER_MINUTE=100
RATE_LIMIT_LOGIN_PER_MINUTE=5

# Token 加密配置
TOKEN_ENCRYPTION_ENABLED=false
ENCRYPTION_KEY=mt_coupon_encryption_32_chars_key
